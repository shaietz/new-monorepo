/** @public — nothing imports this package yet; the tag is what keeps knip quiet until something does. */
export { greeting } from "./greeting.ts";

/** @public — the options bag callers pass to `greeting`. */
export type { GreetingOptions } from "./greeting.ts";
