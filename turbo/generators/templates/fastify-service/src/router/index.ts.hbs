import { apiRouter } from './routers/api.ts';
import { router } from '@repo/{{ dashCase name }}-api';

export const appRouter = router({
  api: apiRouter,
});

export type AppRouter = typeof appRouter;